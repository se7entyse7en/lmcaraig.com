import React from "react"
import Navbar from "../components/navbar"

export default ({data}) => (
  <React.Fragment>
    <Navbar></Navbar>
    <div className="container">
      <p>It's me!</p>
    </div>
  </React.Fragment>
)
